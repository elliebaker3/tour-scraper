# crontab -e   (times are the machine's local time)
# Start capture 15 min before stage start each day in July:
45 11 * 7 * cd $HOME/tour-scraper && /usr/bin/python3 -m tourscraper stage --max-hours 6 >> /tmp/tour-scraper.log 2>&1
